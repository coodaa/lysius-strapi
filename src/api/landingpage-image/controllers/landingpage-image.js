'use strict';

/**
 * landingpage-image controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::landingpage-image.landingpage-image');
