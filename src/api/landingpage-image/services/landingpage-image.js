'use strict';

/**
 * landingpage-image service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::landingpage-image.landingpage-image');
